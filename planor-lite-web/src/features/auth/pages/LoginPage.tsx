export const LoginPage: React.FC = () => {
  return (
    <main className="min-h-screen w-full grid grid-cols-1 lg:grid-cols-2">
      {/* Left Side - Quote Carousel (hidden on mobile) */}
      <h1>Hola mundo!</h1>
    </main>
  )
}